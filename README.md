# DataInsight

Java 应用程序开发课程实验项目。实验一包含欢迎程序和成绩计算程序。

本机切换到 JDK 17 后，在 src 目录编译运行：

    source ../env.sh
    javac -encoding UTF-8 DataInsight.java ScoreCalc.java
    java DataInsight
    java ScoreCalc

编译生成的 class 文件及本地 IDE 配置已加入 .gitignore。
